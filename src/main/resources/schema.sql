CREATE TABLE `test_snow_flake0`
(
    `id`   bigint,
    `name` varchar(45) NOT NULL,
    PRIMARY KEY (`id`)
);


CREATE TABLE `test_snow_flake1`
(
    `id`   bigint,
    `name` varchar(45) NOT NULL,
    PRIMARY KEY (`id`)
);
